# 开发环境配置文件
# .env.development 用于定义开发环境下的环境变量
# Vite 是一个现代化的前端构建工具，提供快速的开发服务器和优化的生产构建
# Vite 会根据不同的模式自动加载对应的环境文件：
# - .env.development (开发环境，npm run dev)
# - .env.test (测试环境，npm run build:test)  
# - .env.production (生产环境，npm run build)

# 应用部署域名（nginx 服务，80 端口）
VITE_DEPLOY_DOMAIN=http://localhost

# API 基础地址（后端服务）
VITE_API_BASE_URL=/api
